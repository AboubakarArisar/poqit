# Privacy

POQIT is local-first. It does not require an account, upload shelf contents, or include an analytics SDK. File and folder items are references to their original Windows paths. Text and URLs are stored in the local SQLite database.

Clipboard monitoring is not enabled by default. Operational logs must never include clipboard values, full stored text, or secrets.
