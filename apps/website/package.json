{
  "name": "@poqit/website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": { "dev": "vite --host 127.0.0.1", "build": "tsc && vite build", "check": "tsc --noEmit --pretty false" },
  "devDependencies": { "typescript": "^5.9.2", "vite": "^7.1.5" }
}
